# 阶段性交付验收清单（P0）

> 目标：把“已完成”转成“可重复验收”。
> 适用版本：v1.8.3 当前阶段交付节点

---

## 一、构建与基础质量

### 1. 本地基础检查
- [ ] `./gradlew clean`
- [ ] `./gradlew lintDebug --continue`
- [ ] `./gradlew testDebugUnitTest --continue`
- [ ] `./gradlew assembleDebug`
- [ ] `./gradlew assembleRelease`

### 2. 构建产物检查
- [ ] 存在 `app/build/outputs/apk/debug/app-debug.apk`
- [ ] 存在 `app/build/outputs/apk/release/app-release.apk` 或 `app-release-unsigned.apk`
- [ ] Release 构建未出现阻塞性错误

### 3. CI / 工作流检查
- [ ] GitHub Actions `Lint Check` 成功
- [ ] GitHub Actions `Unit Tests` 成功
- [ ] GitHub Actions `Build APK` 成功
- [ ] 若打 tag，`release` job 成功
- [ ] artifacts 中可下载 debug / release APK

---

## 二、问题1：OCR 图片识别链路验收

### 1. 正常图片识别
- [ ] 选择清晰账单图片后可正常进入 OCR 流程
- [ ] 可识别出金额/时间/商户等关键信息中的至少一部分
- [ ] 中高置信度结果可继续送入 AI 处理

### 2. 低质量图片处理
- [ ] 选择模糊/过暗/严重遮挡图片时，不会盲目继续执行
- [ ] 低置信度场景会提示用户重拍或重试

### 3. 稳定性
- [ ] 多次使用图片识别不会出现明显卡死或崩溃
- [ ] 不同图片样本下结果表现基本稳定

---

## 三、问题2：AUTO 模型推荐与调度验收

### 1. 模型配置展示
- [ ] AI 设置页可正常展示模型列表
- [ ] 推荐信息可正常展示
- [ ] 推荐摘要与智能路由建议分离展示

### 2. AUTO 模式连接测试
- [ ] 在 AUTO 模式下点击测试连接可得到结果
- [ ] 首候选失败时可触发备用候选尝试
- [ ] 测试结束后页面不会一直卡在 testing 状态

### 3. FIXED 模式语义
- [ ] 固定模型不可用时不会偷偷切换模型
- [ ] 会返回明确失败提示

---

## 四、问题3：指令理解与执行链路验收

### 1. 普通文本记账
- [ ] 输入明确记账语句可正常解析并执行
- [ ] 普通对话不会被误判为记账

### 2. clarification 澄清闭环
- [ ] 缺金额时会先追问金额
- [ ] 缺交易类型时会先追问收入/支出/转账
- [ ] 多账户未指明账户时会先追问账户
- [ ] 分类缺失时会先追问分类
- [ ] 日期缺失时会先追问日期
- [ ] 用户补充后可沿上下文继续执行

### 3. confirmation 确认闭环
- [ ] 修改类操作可进入待确认状态
- [ ] 确认后可继续执行
- [ ] 失败或取消时 pending 状态行为符合预期

### 4. 结构化执行稳定性
- [ ] 纯文本回复不会误触发 JSON action 执行
- [ ] explanatory JSON 示例不会被误执行
- [ ] 多轮 clarification 不会丢失最初原始意图

---

## 五、问题4：网络韧性与路由验收

### 1. 网络测速
- [ ] AI 设置页可触发网络测速
- [ ] 可得到结构化测速结果
- [ ] 测速结果可用于生成推荐

### 2. 重试与退避
- [ ] 短暂网络波动下，请求存在自动重试行为
- [ ] 不会因短时失败直接进入不可恢复状态

### 3. 条件刷新与压缩传输
- [ ] 模型列表请求可正常获取
- [ ] 缓存/304 场景不会导致列表异常
- [ ] 非流式请求链路保持可用

### 4. 网络状态预警
- [ ] 离线状态有预警提示
- [ ] 测速失败有预警提示
- [ ] 高延迟有预警提示
- [ ] provider / API URL / 网关变化后旧状态可被清理

---

## 六、配套能力验收

### 1. 模型测试按钮
- [ ] 模型选择界面存在测试连接按钮
- [ ] 点击后能给出成功或失败结果

### 2. 邀请码绑定
- [ ] 绑定成功后可写入 token / API 地址
- [ ] 绑定后模型模式保持 Auto 自动优选

### 3. Excel 导出
- [ ] 可执行正常导出
- [ ] 空数据导出不会崩溃
- [ ] 汇总导出可正常工作

### 4. Release 相关
- [ ] Release APK 可成功生成
- [ ] 手工安装验证通过
- [ ] 当前 Apache POI / R8 warning 保持非阻塞

---

## 七、已知非阻塞事项

- [ ] 已确认旧 `workers.dev` 路径迁移逻辑不影响当前主流程
- [ ] 已确认本地 bash `uname: command not found` 日志不阻塞质量检查
- [ ] 已确认 Apache POI / R8 warning 当前属于非阻塞项

---

## 八、验收结论

- [ ] 本阶段交付验收通过
- [ ] 可进入下一阶段优化
- [ ] 若存在问题，记录阻塞项并回到对应模块修复
